// Route-level page exports kept here so the app entry remains a small composition root.
// These can be migrated one-by-one from the legacy UI module without changing routes.
export { ContactsPage, GeographyPage, HomePage, MethodologyPage, NotFoundPage, SourceDetailPage, TimelinePage } from './pageAdapters';
